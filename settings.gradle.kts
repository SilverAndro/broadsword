rootProject.name = "broadsword"

pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            name = "QuiltMC"
            url = uri("https://maven.quiltmc.org/repository/release/")
        }
    }
}